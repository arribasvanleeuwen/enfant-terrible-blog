# Configuración Principal
title: "ENFANT TERRIBLE"
description: "Editorial insights on next-gen graphics and game technology. The written extension of ENFANT TERRIBLE's gaming channel." # Añade una descripción breve
url: "https://www.enfant-terrible.media" # Importante para que el RSS sepa su dirección
baseurl: "" 

# Apariencia
remote_theme: jekyll/minima
minima:
  skin: dark

# Plugins
plugins:
  - jekyll-remote-theme
  - jekyll-feed

# Configuración del Feed (Añade esto para que se genere en la raíz)
feed:
  path: /feed.xml
